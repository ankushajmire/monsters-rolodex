name: s3-deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ monster.secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ monster.secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ monster.secrets.AWS_REGION }}
      - name: Build React App
        run: npm install && npm run build
      - name: Deploy app build to S3 bucket
        run: aws s3 sync ./build/ s3://${{ monster.secrets.AWS_BUCKET_NAME }} --delete



